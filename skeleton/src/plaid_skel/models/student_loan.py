# coding: utf-8
# AUTOGENERATED with MDM customization.  Do not edit.




from __future__ import annotations
from datetime import date, datetime  # noqa: F401

import re  # noqa: F401
from typing import Any, Dict, List, Optional  # noqa: F401

from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator  # noqa: F401
from plaid_skel.models.pslf_status import PSLFStatus
from plaid_skel.models.servicer_address_data import ServicerAddressData
from plaid_skel.models.student_loan_status import StudentLoanStatus
from plaid_skel.models.student_repayment_plan import StudentRepaymentPlan




class StudentLoan(BaseModel):
    """Contains details about a student loan account"""

    class Config:
        schema_extra = {
            "externalDocs": {
                "url": "https://plaid.com/docs/api/accounts/#student_loan"
            }
        }

    account_id: Optional[str] = Field(default=None, description="The ID of the account that this liability belongs to.")
    account_number: Optional[str] = Field(default=None, description="The account number of the loan. For some institutions, this may be a masked version of the number (e.g., the last 4 digits instead of the entire number).")
    disbursement_dates: Optional[List[date]] = Field(default=None, description="The dates on which loaned funds were disbursed or will be disbursed. These are often in the past. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD).")
    expected_payoff_date: Optional[date] = Field(default=None, description="The date when the student loan is expected to be paid off. Availability for this field is limited. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD).")
    guarantor: Optional[str] = Field(default=None, description="The guarantor of the student loan.")
    interest_rate_percentage: float = Field( description="The interest rate on the loan as a percentage.")
    is_overdue: Optional[bool] = Field(default=None, description="`true` if a payment is currently overdue. Availability for this field is limited.")
    last_payment_amount: Optional[float] = Field(default=None, description="The amount of the last payment.")
    last_payment_date: Optional[date] = Field(default=None, description="The date of the last payment. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD).")
    last_statement_issue_date: Optional[date] = Field(default=None, description="The date of the last statement. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD).")
    loan_name: Optional[str] = Field(default=None, description="The type of loan, e.g., \"Consolidation Loans\".")
    loan_status: StudentLoanStatus = Field()
    minimum_payment_amount: Optional[float] = Field(default=None, description="The minimum payment due for the next billing cycle. There are some exceptions: Some institutions require a minimum payment across all loans associated with an account number. Our API presents that same minimum payment amount on each loan. The institutions that do this are: Great Lakes ( `ins_116861`), Firstmark (`ins_116295`), Commonbond Firstmark Services (`ins_116950`), Nelnet (`ins_116528`), EdFinancial Services (`ins_116304`), Granite State (`ins_116308`), and Oklahoma Student Loan Authority (`ins_116945`). Firstmark (`ins_116295` ) and Navient (`ins_116248`) will display as $0 if there is an autopay program in effect.")
    next_payment_due_date: Optional[date] = Field(default=None, description="The due date for the next payment. The due date is `null` if a payment is not expected. A payment is not expected if `loan_status.type` is `deferment`, `in_school`, `consolidated`, `paid in full`, or `transferred`. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD).")
    origination_date: Optional[date] = Field(default=None, description="The date on which the loan was initially lent. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD). ")
    origination_principal_amount: Optional[float] = Field(default=None, description="The original principal balance of the loan.")
    outstanding_interest_amount: Optional[float] = Field(default=None, description="The total dollar amount of the accrued interest balance. For Sallie Mae ( `ins_116944`), this amount is included in the current balance of the loan, so this field will return as `null`.")
    payment_reference_number: Optional[str] = Field(default=None, description="The relevant account number that should be used to reference this loan for payments. In the majority of cases, `payment_reference_number` will match `account_number,` but in some institutions, such as Great Lakes (`ins_116861`), it will be different.")
    pslf_status: PSLFStatus = Field()
    repayment_plan: StudentRepaymentPlan = Field()
    sequence_number: Optional[str] = Field(default=None, description="The sequence number of the student loan. Heartland ECSI (`ins_116948`) does not make this field available.")
    servicer_address: ServicerAddressData = Field()
    ytd_interest_paid: Optional[float] = Field(default=None, description="The year to date (YTD) interest paid. Availability for this field is limited.")
    ytd_principal_paid: Optional[float] = Field(default=None, description="The year to date (YTD) principal paid. Availability for this field is limited.")

StudentLoan.update_forward_refs()
