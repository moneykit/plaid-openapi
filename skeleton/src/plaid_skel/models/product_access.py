# coding: utf-8
# AUTOGENERATED with MDM customization.  Do not edit.




from __future__ import annotations
from datetime import date, datetime  # noqa: F401

import re  # noqa: F401
from typing import Any, Dict, List, Optional  # noqa: F401

from pydantic import AnyUrl, BaseModel, EmailStr, Field, validator  # noqa: F401




class ProductAccess(BaseModel):
    """The product access being requested. Used to or disallow product access across all accounts. If unset, defaults to all products allowed."""

    class Config:
        schema_extra = {
            "externalDocs": {
                "url": "https://plaid.com/docs/api/accounts/#product_access"
            }
        }

    statements: Optional[bool] = Field(default=None, description="Allow access to statements. Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    identity: Optional[bool] = Field(default=None, description="Allow access to the Identity product (name, email, phone, address). Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    auth: Optional[bool] = Field(default=None, description="Allow access to account number details. Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    transactions: Optional[bool] = Field(default=None, description="Allow access to transaction details. Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    accounts_details_transactions: Optional[bool] = Field(default=None, description="Allow access to \"accounts_details_transactions\". Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    accounts_routing_number: Optional[bool] = Field(default=None, description="Allow access to \"accounts_routing_number\". Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    accounts_statements: Optional[bool] = Field(default=None, description="Allow access to \"accounts_statements\". Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    accounts_tax_statements: Optional[bool] = Field(default=None, description="Allow access to \"accounts_tax_statements\". Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")
    customers_profiles: Optional[bool] = Field(default=None, description="Allow access to \"customers_profiles\". Only used by certain partners. If relevant to the partner and unset, defaults to `true`.")

ProductAccess.update_forward_refs()
